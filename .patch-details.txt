Patch: fix($locale head): avoid hook usage inside head callback

Files changed:
- src/routes/$locale.tsx

Summary:
- Replaced an invalid call to Route.useLoaderData() inside the route's
  `head` callback with the supported pattern: read `loaderData` from the
  `head` callback args. Kept useLoaderData() inside the route component
  (LocaleLayout) where React hooks are allowed.

Build: see logs returned by npm run build
